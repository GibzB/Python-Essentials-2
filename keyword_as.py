# Also referref to as ALIASING

import math as m

print(m.sin(m.pi/2))

# when you use the from module import name variant and you need to change the entity's name, you make an alias for the entity.

