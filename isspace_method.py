# isspace() method
# The isspace() method returns True if there are only whitespace characters in the string. If not, it returns False.
# The isspace() method identifies whitespaces only – it disregards any other character

# Example: Demonstrating the isspace() method:
print(' \n '.isspace())
print(" ".isspace())
print("mooo mooo mooo".isspace())
